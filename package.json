{
  "name": "elysia-api",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build-standalone.mjs",
    "build:webui": "yarn workspace @root/webui build",
    "lint": "yarn eslint packages/webui --cache --ext=ts,tsx",
    "lint-fix": "yarn eslint packages/webui --cache --ext=ts,tsx --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "typescript": "^5.9.2"
  }
}
